<%
  var featuredProjects = Array.isArray(site.data.projects)
    ? site.data.projects.filter(function(project) { return project.featured; })
    : [];
  var recentPosts = site.posts && typeof site.posts.toArray === 'function'
    ? site.posts.toArray().sort(function(a, b) {
        return new Date(b.date) - new Date(a.date);
      }).slice(0, 5)
    : [];
  var socialLinks = theme.social_links || {};
  var socialKeys = Object.keys(socialLinks);
%>

<section class="home-hero">
<p class="home-social">
  Find me on
  <% socialKeys.forEach(function(name) { %>
    <% var iconClass = name === 'mail' ? 'fas fa-envelope' : 'fab fa-' + name; %>
    <a class="icon" href="<%= socialLinks[name] %>"<% if (name !== 'mail') { %> target="_blank" rel="noopener me"<% } %> aria-label="<%= name === 'twitter' ? 'εlt (@256elt) / X' : name %>" title="<%= name === 'twitter' ? 'εlt (@256elt) / X' : name %>"><% if (name === 'twitter') { %><span aria-hidden="true">𝕏</span><% } else { %><i class="<%= iconClass %>" aria-hidden="true"></i><% } %></a>
  <% }) %>
  .
</p>
</section>

<section class="home-section">
  <p class="section-label">Recent Writing</p>
  <ul class="post-list">
    <% if (recentPosts.length) { %>
      <% recentPosts.forEach(function(post) { %>
        <li class="post-item">
          <%- partial('_partial/post/date', { post: post, class_name: 'meta' }) %>
          <span><%- partial('_partial/post/title', { post: post, index: true, class_name: '' }) %></span>
        </li>
      <% }) %>
    <% } else { %>
      <li class="post-item">WIP.</li>
    <% } %>
  </ul>
  <% if (!recentPosts.length) { %>
    <p class="section-note">Nothing public yet.</p>
  <% } %>
</section>

<% if (featuredProjects.length) { %>
  <section class="home-section">
    <p class="section-label">Projects</p>
    <ul class="project-list">
      <% featuredProjects.forEach(function(project) { %>
        <li class="project-item">
          <a href="<%= project.url %>" target="_blank" rel="noopener"><%= project.name %></a>: <%= project.summary %>
        </li>
      <% }) %>
    </ul>
  </section>
<% } %>


